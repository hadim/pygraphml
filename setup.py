from distutils.core import setup

setup(name='pygraphml',
    version='1.0',
    author='Hadrien Mary',
    url='https://github.com/hadim/pygraphml/',
    description='Small library to parse GraphML file in Python',
    packages=['pygraphml'])
